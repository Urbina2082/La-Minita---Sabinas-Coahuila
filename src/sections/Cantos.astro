<section class="karaoke" id="karaoke">
  <div class="contenido">
    <div class="encabezado">
      <span>06</span>
      <i></i>
      <small>SÁBADOS</small>
    </div>

    <h2>
      LOS SÁBADOS<br />
      <strong>SE CANTA.</strong>
    </h2>

    <p class="principal">¡Tú eliges la canción, nosotros el ambiente!</p>

    <p class="descripcion">
      Súbete al escenario y demuestra tu talento... o simplemente ven a
      disfrutar.
    </p>

    <div class="botones">
      <a href="#reservar" class="btn-principal"> QUIERO CANTAR </a>

      <a href="#ambiente" class="btn-secundario"> VER AMBIENTE </a>
    </div>
  </div>
</section>

<style>
  .karaoke {
    width: 100%;
    min-height: 650px;

    display: flex;
    align-items: center;

    padding: 100px 12%;

    position: relative;

    background-image:
      linear-gradient(
        90deg,
        rgba(0, 0, 0, 0.95) 0%,
        rgba(0, 0, 0, 0.75) 45%,
        rgba(0, 0, 0, 0.25) 100%
      ),
      url("/kara.avif");

    background-size: cover;
    background-position: center;
  }

  /* CONTENIDO */

  .contenido {
    max-width: 600px;
  }

  /* ENCABEZADO */

  .encabezado {
    display: flex;
    align-items: center;

    gap: 12px;

    margin-bottom: 30px;
  }

  .encabezado span {
    color: #b5965e;

    font-size: 12px;
    letter-spacing: 2px;
  }

  .encabezado i {
    width: 25px;
    height: 1px;

    background: #333;
  }

  .encabezado small {
    color: #777;

    font-size: 12px;
    letter-spacing: 3px;
  }

  /* TITULO */

  h2 {
    margin: 0 0 25px;

    color: #eee;

    font-family: Georgia, serif;

    font-size: 60px;
    font-weight: normal;

    line-height: 0.95;
  }

  h2 strong {
    color: #c9aa70;

    font-weight: normal;
  }

  /* TEXTOS */

  .principal {
    margin: 0 0 15px;

    color: #aaa;

    font-size: 14px;
  }

  .descripcion {
    margin: 0 0 30px;

    color: #777;

    font-size: 13px;
  }

  /* BOTONES */

  .botones {
    display: flex;
    align-items: center;

    gap: 30px;
  }

  .btn-principal {
    padding: 12px 22px;

    color: #c9aa70;

    border: 1px solid #4d4029;
    border-radius: 30px;

    text-decoration: none;

    font-size: 12px;
    letter-spacing: 3px;
  }

  .btn-secundario {
    color: #aaa;

    text-decoration: none;
    padding: 12px 22px;
    font-size: 11px;
    letter-spacing: 3px;
  }

  /* TABLET */

  @media (max-width: 900px) {
    .karaoke {
      min-height: 600px;

      padding: 80px 7%;

      background-position: center;
    }

    h2 {
      font-size: 50px;
    }
  }

  /* CELULAR */

  @media (max-width: 600px) {
    .karaoke {
      min-height: 650px;

      padding: 80px 25px;

      align-items: center;

      background-position: 65% center;
    }

    .contenido {
      max-width: 100%;
    }

    .encabezado {
      margin-bottom: 25px;
    }

    h2 {
      font-size: 42px;

      line-height: 1;
    }

    .principal {
      font-size: 13px;

      line-height: 1.5;
    }

    .descripcion {
      max-width: 320px;

      font-size: 13px;

      line-height: 1.6;
    }

    .botones {
      flex-direction: column;

      align-items: flex-start;

      gap: 18px;
    }
  }

  /* CELULAR PEQUEÑO */

  @media (max-width: 400px) {
    .karaoke {
      min-height: 600px;

      padding: 70px 20px;
    }

    h2 {
      font-size: 39px;
    }

    .principal {
      font-size: 14px;
    }
  }
</style>
